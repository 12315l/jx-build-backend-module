# 首轮模块验收基线

## 验收范围

- 模板基线：`base-framework/src/test/resources/KeyModule`
- 关联业务模块：`CourseOrderModule`
- 库存核销模块：`EquipmentClaimModule`
- 验收日期：2026-07-14
- 验收性质：只读结构与静态一致性检查，未执行 SQL，未修改业务代码

## KeyModule

- 实际包含 Controller、Service、DAO、Entity 和 SQL 共五个文件。
- `Key`、`key` 和 `XX` 占位内容仍存在，符合模板源文件特征。
- 结论：只能用于生成未裁剪骨架，不能作为已完成业务模块交付。
- 已固化规则：必须经过模块命名规范化、能力开关裁剪、真实字段补充、权限对齐和模块验收。

## CourseOrderModule

- 标准五类文件、包路径、表名和模板清理检查通过。
- 实体已有结课成绩与结课结果，但当前建表 SQL 缺少对应两列，构成阻断项。
- 当前结课处理仍调用奖励模块，与最新“取消荣誉奖励”修正冲突，已在项目追踪文件中标记为待移除。
- 模块 SQL 未发现当前 Controller 权限的本地注册依据，需继续核对全局初始化脚本或实际数据库。
- 模块 SQL 含删表语句，只能作为明确的新表初始化脚本，不能自动用于既有数据库升级。
- 结论：当前不能标记为已验证；必须先完成实体与 SQL 对齐并落实奖励移除修正。

## EquipmentClaimModule

- 标准五类文件、包路径、表名和十个实体字段与 SQL 列的一致性检查通过。
- 当前代码存在发放确认、归还申请、归还验收、损坏或丢失核销及流转记录入口。
- 项目追踪文件已将申请不预扣、发放再扣减、归还恢复可用量、报损核减总量和数量守恒列入流程验收条件。
- 模块 SQL 未发现当前 Controller 权限的本地注册依据；后台编译和运行级并发、回滚、越权验证尚未执行。
- 结论：结构基线通过，但在编译、权限来源和业务不变量验证完成前不能标记为全部验证。

## 回归预期

- KeyModule 直接作为交付模块时，模板占位检查必须阻断。
- CourseOrderModule 在 SQL 未补齐结课字段前，实体与 SQL 一致性检查必须阻断。
- EquipmentClaimModule 当前结构检查不得误报字段不一致，但必须保留编译、权限和业务闭环未验证提示。
- 任何模块即使结构检查通过，也不能自动升级为项目级“已验证”；项目追踪验证门禁必须同时通过。

